<!-- rounded_square_background.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="250dp"/> <!-- Độ cong của góc (tùy chỉnh để làm cho hình tròn) -->
    <solid android:color="@android:color/transparent"/> <!-- Đặt màu nền là trong suốt -->
</shape>
